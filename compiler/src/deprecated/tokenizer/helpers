export function returnName(cursorPosition: number, LETTERS: RegExp,character: string, arrayContent: string) {
    let textString = "";

    while (LETTERS.test(character)) {
        textString += character;
        cursorPosition++;
        character = arrayContent[cursorPosition];
    }
    return {
        position: cursorPosition,
        value: textString
    }
}