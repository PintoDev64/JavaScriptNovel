import { describe, it } from "node:test";

describe("Parser", {},() => {
    it("should return a parsed token list {NParser.IToken type}", () => {
        // const tokenList = await Parser(scriptContent);
        // console.log("Parser Result:\n",tokenList);
    });
});